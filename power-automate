Update the Stage 1 (Data Onboarding) intro section with the following changes. Do not modify the badge, icon, title, or any elements below the intro paragraph.
Current intro text:
"Admit the Archer source feeds into the pipeline as the authoritative data set for this run — nothing downstream executes until all three sub-stages below complete."
Replace with:
"This stage admits the source feeds into the pipeline as the authoritative dataset for this run.
Why this matters: every downstream stage operates on the data admitted here. If a file is missing, duplicated, or has the wrong columns, those problems would propagate silently through the pipeline. This is the governance gate — nothing executes downstream until all three sub-stages below complete cleanly."
Render this as two paragraphs with appropriate vertical spacing between them. Use the same typography (sans-serif, regular weight, body text size) as the rest of the page. Bold the phrase "Why this matters" if your design system supports inline emphasis; otherwise leave it as plain text.
Do not modify the Stage 1 · Governance Gate badge, the document icon, the Data Onboarding title, or the blue "What happens when you click Data Onboarding" callout box below this intro section.
