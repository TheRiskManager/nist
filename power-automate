On the Stage 1 (Data Onboarding) page, adjust the font size hierarchy as follows. Do not modify any other styling.

Body intro paragraph (the "This stage admits the source feeds..." / "Why this matters..." text directly under the Data Onboarding title): increase font size by 1–2 pixels (or one step up in the design system) so it clearly reads as the primary descriptive content of the stage.
Blue callout body text (inside the "What happens when you click Data Onboarding" box): decrease font size by 1–2 pixels (or one step down in the design system) so the callout reads as supporting reference information rather than primary content.
Bolded section leads inside the callout ("If something goes wrong", "When all three sub-stages complete"): match the new smaller callout body size, just bolder.
Do not modify: the stage title size, the "STAGE 1 · GOVERNANCE GATE" badge, the "WHAT HAPPENS WHEN YOU CLICK DATA ONBOARDING" header inside the callout, the sub-stage card titles, card body text, button labels, or status pill text.

The goal is to establish clear visual hierarchy: body intro → larger and primary, callout → smaller and secondary.
