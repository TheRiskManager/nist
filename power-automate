Now we need to update 3 things:
1. Flow 3B — add Final Scope Type to the Update Classification With AI action. Open the action, add:

Final Scope Type — click fx: if(equals(outputs('Parse_Scope_Type'), 'Common Control'), 890920000, if(equals(outputs('Parse_Scope_Type'), 'Application-Level Control'), 890920001, 890920002))

(Same expression as Scope Type — AI sets both to the same value initially.)
2. Business Rule 8.6 — on Scope Classifications table:

Condition: Review Decision = Overridden
Action: Set Final Scope Type = Override Scope Type
This way when the risk manager overrides, Final Scope Type automatically updates.

3. Flow 3C trigger — change Filter columns to: pei_finalscopetype

Remove pei_reviewdecision,pei_scopetype
Filter rows: (keep empty, the Is Still Pending condition handles it)

This way Flow 3C fires whenever Final Scope Type changes — whether from AI (Flow 3B sets it) or from override (business rule copies it).
Start with updating Flow 3B. Tell me when done.
