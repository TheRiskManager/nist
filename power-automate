On the Stage 1 (Data Onboarding) page, constrain the width of the blue callout box (containing "What happens when you click Data Onboarding") to a maximum reading width of approximately 720 pixels (or 60% of the parent container width, whichever is smaller). Keep the callout left-aligned or centered within the parent container — match whichever alignment the rest of the page content uses.
Do not change the content, the three-paragraph structure, the bolded section leads, the blue background, or the border styling. Only adjust the container width so the callout no longer leaves a large empty area on the right side.
The goal is for the text inside the callout to wrap at a comfortable reading width (roughly 65–75 characters per line) rather than the current narrow wrap inside an over-wide container.
