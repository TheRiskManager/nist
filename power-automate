Step 1: Update the AI prompt. Go to AI Hub → Prompts → EMS Import Assessment. Change the formatting instruction back to:
IMPORTANT: Format your response in HTML. Use <h3> for section headers, <ul><li> for bullet points, <p> for paragraphs, and <strong> for emphasis. Do not use markdown formatting like ** or - bullets.
Save the prompt.
Step 2: Update Format AI For Email — this now becomes Strip HTML For Form. Click on it, change:
Inputs — fx:
replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(outputs('AI_Import_Assessment')?['body/responsev2/predictionOutput/text'], '<h3>', decodeUriComponent('%0A')), '</h3>', decodeUriComponent('%0A')), '<p>', ''), '</p>', decodeUriComponent('%0A')), '<ul>', ''), '</ul>', ''), '<li>', '- '), '</li>', decodeUriComponent('%0A')), '<strong>', ''), '</strong>', '')
Rename to: Strip HTML For Form
Step 3: Update Save AI Assessment — change Copilot Assessment to:
outputs('Strip_HTML_For_Form')
Step 4: Update Notify Risk Manager — change the AI reference in the email body back to the raw AI output:
outputs('AI_Import_Assessment')?['body/responsev2/predictionOutput/text']
This way:

Email gets the HTML output directly (looks great)
Form gets cleaned plain text (readable)

Do all 4 steps and test. Tell me how both look.





IMPORTANT: Format your response in plain text only. Use clear section headers on their own line followed by a blank line. Use "- " for bullet points. Do not use any HTML tags, markdown formatting, or special characters like ** or ##. The output must be readable as plain text in both emails and form fields.


contains(string(first(body('Read_RC_Excel')?['value'])), 'Reference Control ID')



Name: varWarnings
Value: RC file has invalid headers. Expected 'Reference Control ID' column. File skipped.
Rename to: Warn RC Invalid Header







Inside BP File Exists → If Yes, right after Read BP Excel and before Loop BP Rows, add:

Condition
Left: fx → contains(string(first(body('Read_BP_Excel')?['value'])), 'Business Process ID')
Operator: is equal to
Right: fx → true
Rename to: BP Header Valid

Then move everything (Loop BP Rows, Archive, Removal Detection) into the If Yes branch.
In the If No branch, add:

Append to string variable → varWarnings
Value: BP file has invalid headers. Expected 'Business Process ID' column. File skipped.
Rename to: Warn BP Invalid Header









