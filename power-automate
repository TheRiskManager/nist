2. Replace the stage intro paragraph
Current text:
"Assemble the consolidated Risk Register from all validated Archer feeds — the pipeline runs 10 sequential steps below, fully automatically."
Replace with:
"Build the integrated risk and controls data view from your validated source files. The pipeline runs 10 steps in sequence, fully automatically — each step's outcome appears below as it completes."

3. Rewrite the "What happens..." callout body
Keep the heading "What happens when you click Run Pipeline" unchanged. Keep the callout's blue background, border, and small-caps heading style.
Replace the current callout body with three short paragraphs:
"The pipeline runs 10 steps in sequence, fully automatically. You don't need to do anything once it starts."
"Each step shows green when it completes successfully, or red if it fails. If a step fails, the system shows a clear error message and a Retry button — you can fix the underlying issue and resume from the failed step without re-running the earlier ones."
"When you're ready, click Run Pipeline below."
