C6 — Build CP Classifier (Compose)
Inside If no branch of C4, after C5.

Action: Data Operation → Compose
Title: Build CP Classifier
Inputs (expression):

if(greater(length(outputs('Find_BP_Mapping_for_CP')?['body/value']), 0), if(equals(first(outputs('Find_BP_Mapping_for_CP')?['body/value'])?['pei_ownershiptype'], 890920000), 'bu-owned', if(equals(first(outputs('Find_BP_Mapping_for_CP')?['body/value'])?['pei_ownershiptype'], 890920001), 'horizontal', 'unresolved')), 'missing')
Save. Tell me "done" or paste error.
