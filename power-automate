Action 8: Inside CP Found → If Yes, add Dataverse → Add a new row

Table: Scope Classifications
Control Procedure — click fx: concat('pei_controlprocedures(', first(outputs('Find_CP_Record')?['body/value'])?['pei_controlprocedureid'], ')')
Import Cycle — click fx: concat('pei_importcycles(', triggerOutputs()?['body/pei_importcycleid'], ')')
Review Decision: Pending (dropdown)
Rename to: Create Scope Classification
