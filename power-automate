On the Stage 1 (Data Onboarding) page, revert the blue callout box back to a single-block layout. Remove the three-column structure and the "What runs" / "If something goes wrong" / "When all three sub-stages complete" column headings.
Replace the current three-column layout with a single full-width block containing one continuous body. Keep the callout edge-to-edge in width, matching the body intro paragraph above it. Do not constrain it to a narrow center column.
Callout content:
Heading (small caps, letter-spaced, blue, unchanged): "WHAT HAPPENS WHEN YOU CLICK DATA ONBOARDING"
Body (single block, three short paragraphs flowing one after the other):
"The pipeline runs three sub-stages back-to-back — Check → Load → Validate — fully automatically. See the cards below for what each one does."
"If something goes wrong: the flow pauses on the affected card so you can fix the issue (drop in the right file, pick a version, correct columns). Your completed steps are preserved — just press the button again to resume."
"When all three sub-stages complete: the next stage, Run Pipeline, becomes available."
Render the three paragraphs stacked vertically with normal paragraph spacing between them. Use bolded inline section leads ("If something goes wrong:", "When all three sub-stages complete:") at the start of paragraphs 2 and 3 — these are inline emphasis, not separate column headings.
Keep the existing blue background, blue left border, and the small-caps heading. The callout container should run full-width, edge-to-edge, matching the body intro paragraph.
Update the stage page design specification to reflect this as the canonical callout layout: single full-width block, three stacked paragraphs with bolded inline section leads, no column structure.
