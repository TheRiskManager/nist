Trim the "What happens when you click Start" callout on the Stage 7 (CAP & BU Reconciliation) page. The callout currently has three paragraphs that include detailed explanation of the OK and Unassigned statuses. With the simplified two-status model, this level of status explanation in the callout is unnecessary — the reviewer will immediately understand the statuses when they see the table. The callout should focus on the workflow setup and the downstream Power BI impact, not on defining the statuses.
Do not modify the body intro paragraphs, the sub-stage cards, the inputs panel, the review table, the filter chips, or any backend behavior.

1. Replace the callout body
Keep the heading "What happens when you click Start" and the existing visual treatment.
Replace the three-paragraph body with two shorter paragraphs:
Paragraph 1: "Clicking Start reveals the upload area, where you'll drop the previous CAP→BU mapping — the historical assignment file from the prior run. This is the only file you need to provide at this stage. The other two inputs — the fresh CAP list from Stage 3 and the current authoritative BU list from Stage 6 — are sourced automatically, so you don't need to manage them here."
Paragraph 2: "Once your file is uploaded, the pipeline confirms all three inputs are in place, reconciles them, and produces an editable review table. Any CAP that needs a business unit assigned will be clearly flagged. When you save, the reconciled mapping becomes the trusted CAP→BU dimension that feeds the Power BI semantic model — every BU-segmented CAP report, scorecard, and dashboard downstream reads from this mapping the next time it refreshes."
Remove the previous third paragraph that explained the statuses in detail (it described what reviewers do with OK and Unassigned rows). That content is no longer needed in the callout — the table itself will communicate this to reviewers when they see it.

2. Optionally add a one-line status hint above the review table
Just above the filter chips and review table, add a small contextual hint in subdued typography:
"Rows marked Unassigned need a business unit. Rows marked OK carry forward unchanged."
This puts the status explanation at the point of need (when the user is about to interact with the table) rather than upfront in the callout.

3. What NOT to change

The two-paragraph body intro above the callout
The 5 sub-stage cards
The inputs panel
The filter chips, the review table columns, or any table-level behavior
The status vocabulary (OK and Unassigned remain)
The engine logic


Update the descriptions on the five sub-stage cards on the Stage 7 (CAP & BU Reconciliation) page. The current descriptions mix accurate user-facing language with internal implementation details that don't serve the reviewer. Update them to consistently describe what the reviewer gets out of each step, not the internal mechanics of how the pipeline does it.
Do not modify the card titles (Upload, Check, Load, Reconcile, Review & Edit), the card numbering, the progressive state behavior, the icons, or any other element on the page.

Card 1 — Upload
Keep current text unchanged:
"Drop the previous CAP→BU mapping here. Click Proceed when ready."

Card 2 — Check
Current text:
"Confirm all three source files are present in the working folder. Pauses here with a fixable message if any one is missing."
Replace with:
"Confirm all three inputs are in place: the previous mapping you just uploaded, the fresh CAP list from Stage 3, and the current BU list from Stage 6. Pauses with a clear message if anything is missing."

Card 3 — Load
Current text:
"Read the mapping plus the 14 enrichment columns from the pipeline output (deduped by CAP ID) and the BU catalogue."
Replace with:
"Read all three inputs into memory in preparation for reconciliation."

Card 4 — Reconcile
Current text:
"For each CAP in Stage 3, validate its previous BU against the current Stage 6 list and tag the row as OK or Unassigned."
Replace with:
"For each CAP in the Stage 3 output, look up its previous BU and confirm the BU is still in the current Stage 6 list. Tag each row OK or Unassigned."

Card 5 — Review & Edit
Current text:
"Resolve every Unassigned row, then Save to write the audit workbook and refresh the baseline."
Replace with:
"Resolve every Unassigned row by picking a business unit from the dropdown, then save to commit the reconciled mapping."

What NOT to change

The card titles (Upload, Check, Load, Reconcile, Review & Edit)
The card numbering, ordering, or visual layout
The progressive state behavior (active, completed checkmarks)
The icons or color treatment on each card
Any other element on the page
The engine logic or backend behavior

