═══════════════════════════════════════════════════════════════════
BLOCK 4 — Create alternate key
        AK_RuleVersion_Rule_VersionNumber
        on (pei_rule + pei_versionnumber)
═══════════════════════════════════════════════════════════════════

WHERE: Power Apps maker → Tables → pei_ruleversion
       → Keys (left rail of the table editor; if you don't see
         "Keys" directly, it may be under "... More" or under the
         "Schema" section depending on the portal layout)
       → + New key


─── KEY DEFINITION ──────────────────────────────────────────────

Display name:        Rule + Version Number
                     (human-readable label for the key)

Name (schema):       AK_RuleVersion_Rule_VersionNumber
                     (verify — should read exactly this; the portal
                      may auto-derive a different name from the
                      display name. Override it if needed.)

Columns:             Add BOTH of these (order matters less than
                     completeness, but pick Rule first then
                     Version Number to match the spec line 71):

                     1. Rule  (the pei_rule lookup column)
                     2. Version Number  (pei_versionnumber)

                     ★ Use the column picker — both columns must
                       appear in the "Selected columns" list before
                       you save. If either is missing, the key
                       won't enforce uniqueness on the pair.


─── SAVE ────────────────────────────────────────────────────────

Click Save.

★ Wait for index provisioning to COMPLETE before moving on.
  The portal usually shows a status like "Active" or a spinner
  that resolves to a green check. This can take 30-90 seconds
  for a new alternate key (Dataverse provisions a unique index
  in SQL behind the scenes). Don't proceed until status = Active
  (or equivalent "ready" indicator).

  If the key shows "Failed" or stays "In progress" for over 2-3
  minutes, STOP and tell me — we'll diagnose before continuing.


═══════════════════════════════════════════════════════════════════
VERIFY BEFORE REPORTING:

  [ ] Key created with name AK_RuleVersion_Rule_VersionNumber
  [ ] Columns on the key = (pei_rule, pei_versionnumber) — both
  [ ] Status = Active (index provisioned successfully)

═══════════════════════════════════════════════════════════════════
