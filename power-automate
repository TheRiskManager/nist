Action 5: Inside Has Existing Records → If Yes, add Apply to each

Select output — click fx: outputs('List_Existing_Applicability')?['body/value']
Rename to: Loop Existing Applicability

Action 6: Inside Loop Existing Applicability, add Dataverse → Update a row

Table: Applicability
Row ID — click fx: items('Loop_Existing_Applicability')?['pei_applicabilityid']
Applicability Status: Inactive (dropdown)
Rename to: Deactivate Old Applicability

Action 7: After the Has Existing Records condition (outside it, below it), add a Condition

Left — click fx: or(equals(triggerOutputs()?['body/pei_scopetype'], 890920000), equals(triggerOutputs()?['body/pei_scopetype'], 890920002))
Operator: is equal to
Right — click fx: true
Rename to: Needs Enterprise Record

NOTE: This checks if Scope Type is Common Control (890920000) OR Hybrid (890920002). If yes → create Enterprise Applicability record.
