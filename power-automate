F4A-39 — Scope: Try Create Evidence Folder
Inside Loop Active Applicability, AFTER the No Evidence Exists condition block (sibling level — runs for every Applicability, not just new Gaps). Control → Scope.
Rename to: Try Create Evidence Folder

F4A-40 — Create new folder
Inside the Scope. SharePoint → Create new folder.
Site Address: https://ems.sharepoint.com/sites/EMS
List or Library: EMS
Folder Path: fx → concat('/Evidence/', items('Loop_Active_Applicability')?['_pei_application_value@OData.Community.Display.V1.FormattedValue'], '/', items('Loop_Active_Applicability')?['_pei_controlprocedure_value@OData.Community.Display.V1.FormattedValue'], '/')
Rename to: Create Evidence Folder
Note: @OData.Community.Display.V1.FormattedValue pulls the lookup's primary field. For App lookup that's Display Name; for CP lookup that's CP Identifier (both are each table's primary name field).

F4A-41 — Compose: Continue On Folder Result
After the Scope (sibling level — still inside Loop Active Applicability, after the Scope). Data Operation → Compose.
Inputs: Continue
Configure Run After on this Compose action — click the "..." on the Compose → Configure run after → check all 3 boxes:

Try Create Evidence Folder is successful
Try Create Evidence Folder has failed
Try Create Evidence Folder is skipped

Save.
Rename to: Continue On Folder Result
