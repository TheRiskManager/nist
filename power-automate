Flow 2, Action 4: Stage 5 validation logic
Inside Stage 5 Confirmed → If Yes, we need to check if all 4 prior gates are Yes. This is a simple condition — no Dataverse query needed.

Inside Stage 5 Confirmed → If Yes, add a Condition
Left side — click fx and enter:

and(triggerOutputs()?['body/pei_impactreviewed'],triggerOutputs()?['body/pei_classificationcomplete'],triggerOutputs()?['body/pei_applicabilityconfirmed'],triggerOutputs()?['body/pei_evidencereviewed'])

Operator: is equal to
Right side — click fx and enter: true
Rename to: All Prior Gates Complete

