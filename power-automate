Action 1: Create the flow:

Power Automate → + New flow → Automated cloud flow
Flow name: EMS Create Applicability
Trigger: Microsoft Dataverse → When a row is modified
Table: Scope Classifications
Scope: Organization
Filter columns: pei_reviewdecision
Filter rows: pei_reviewdecision ne 890920000

Action 2: Add Dataverse → Get a row by ID

Table: Scope Classifications
Row ID: lightning bolt → select the Scope Classification identifier from the trigger
Rename to: Get Classification Record

Action 3: Add Dataverse → List rows

Table: Applicability
Filter rows — click fx: concat('_pei_controlprocedure_value eq ', triggerOutputs()?['body/_pei_controlprocedure_value'], ' and _pei_importcycle_value eq ', triggerOutputs()?['body/_pei_importcycle_value'], ' and pei_applicabilitystatus eq 890920000')
Rename to: List Existing Applicability

Action 4: Add a Condition

Left — click fx: length(outputs('List_Existing_Applicability')?['body/value'])
Operator: is greater than
Right: 0
Rename to: Has Existing Records

Tell me when all 4 are done.
