F4A-19 — Loop Applicability For Removed CP
Inside Removed CP Found → If Yes, after List Applicability For Removed CP. Apply to each.
Select output: fx → outputs('List_Applicability_For_Removed_CP')?['body/value']
Rename to: Loop Applicability For Removed CP

F4A-20 — Deactivate Removed CP Applicability
Inside Loop Applicability For Removed CP. Dataverse → Update a row.
Table: Applicability
Row ID: fx → items('Loop_Applicability_For_Removed_CP')?['pei_applicabilityid']
Applicability Status: Inactive (dropdown — 890920001)
Leave every other field blank.
Rename to: Deactivate Removed CP Applicability
Note: items() inside a loop gives a single object, not an array — direct field access works (no first() needed). Same pattern as F3C-6.

F4A-21 — List Evidence For Removed CP
Inside Removed CP Found → If Yes, after Loop Applicability For Removed CP (sibling, not nested). Dataverse → List rows.
Table: Evidence
Filter rows: fx → concat('_pei_controlprocedure_value eq ', first(outputs('Find_Removed_CP_Record')?['body/value'])?['pei_controlprocedureid'])
Rename to: List Evidence For Removed CP
Note: All Evidence for this CP across all cycles. No status filter — update is idempotent; updating Evidence already Not Required just reassigns the same value. Simpler than ne filters.

F4A-22 — Loop Evidence For Removed CP
After List Evidence For Removed CP. Apply to each.
Select output: fx → outputs('List_Evidence_For_Removed_CP')?['body/value']
Rename to: Loop Evidence For Removed CP

F4A-23 — Mark Evidence Not Required (CP)
Inside Loop Evidence For Removed CP. Dataverse → Update a row.
Table: Evidence
Row ID: fx → items('Loop_Evidence_For_Removed_CP')?['pei_evidenceid']
Evidence Status: Not Required (dropdown — 890920004)
Leave every other field blank.
Rename to: Mark Evidence Not Required (CP)
