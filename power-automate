2. Replace the body intro paragraph
Current:
"The pipeline has assembled all 8 feeds into a single, consolidated, deduplicated, and enriched Risk Register workbook ready for review."
Replace with:
"The pipeline has built the integrated risk and controls data view from all 8 source files. The output is ready in the run folder — open it for review, download a copy, or continue to the next stage when you're ready."

3. Rewrite the "What happens..." callout
Update the heading from "What happens when you click Review Output" to "What you can do here". The previous heading no longer describes the page accurately because there is no single "Review Output" action.
Replace the callout body with three short paragraphs:
"The output is ready in the run folder. You can open it in Excel for review, download a copy to share with stakeholders, or continue to the next stage when you're ready."
"The summary cards below show the row count, column count, when the file was generated, and how long the pipeline took to run."
"Opening or downloading is optional — you can continue without doing either."
Keep the existing callout styling (background color, border, small-caps heading treatment) unchanged.

4. Update the status pill
The current page has two pill states ("Ready to review" before click, "Review the output below — continue when ready" after click). Since there is no longer a forced sequence, replace both with a single persistent pill:
"Output ready · open, download, or continue when ready"

5. File path bar (unchanged from previous prompt)
The bar showing the full path of the output file should still be improved:

Add a label "File location" above or to the left of the path
Display the path in a monospace font
Truncate gracefully with an ellipsis if the path exceeds available width, with the full path in a tooltip on hover
Add a copy-to-clipboard icon at the right end of the bar


6. Summary cards (unchanged from previous prompt)
The four summary cards (Rows, Columns, Generated, Run time) should:

Use sentence case throughout — change "Run Time" to "Run time"
Include the year in the "Generated" value: "May 10, 2026 · 1:17 PM"

Keep the layout, colors, and check icons.

7. Apply the design specification's language conventions
Replace any remaining instances of:

"Risk Register" → "risk and controls data view"
"feeds" → "source files"
"workbook" → "output"
"enriched" → remove
"morph" / "morphs into" → remove
"local Excel instance" → "Excel"




