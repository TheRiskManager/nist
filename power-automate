Action 10: Add Compose

Inputs — click fx: trim(first(split(last(split(outputs('AI_Classify_CP')?['responsePrediction'], 'SCOPE_TYPE: ')), decodeUriComponent('%0A'))))
Rename to: Parse Scope Type

Action 11: Add Compose

Inputs — click fx: trim(first(split(last(split(outputs('AI_Classify_CP')?['responsePrediction'], 'CONFIDENCE: ')), decodeUriComponent('%0A'))))
Rename to: Parse Confidence

Action 12: Add Compose

Inputs — click fx: trim(first(split(last(split(outputs('AI_Classify_CP')?['responsePrediction'], 'REASONING: ')), decodeUriComponent('%0A'))))
Rename to: Parse Reasoning
