Let's pull back the curtain. AI Hub — the platform's prompt registry. Here's the actual prompt the AI uses: defined, version-controlled, auditable. The platform isn't locked to one model — we can swap models and test how each performs. We pick a model, run it. Pick another, run it. The model is hosted in Azure — enterprise infrastructure, not a black box. The capability is proven; the same pattern can be applied wherever judgment-assist is useful, including the Evidence Copilot Assessment surface you saw earlier.
