═══════════════════════════════════════════════════════════════════
BLOCK 2 — Add columns 1-4
        (Rule lookup · Version Number · Conditions JSON Snapshot
         · Output Snapshot)
═══════════════════════════════════════════════════════════════════

WHERE: You should now be in the table editor for pei_ruleversion.
       Click "+ New column" for each column below, one at a time.


─── COLUMN 1: Rule (lookup to pei_rule) ──────────────────────────

Display name:        Rule
Schema name:         pei_rule
                     (verify — the dialog auto-derives from display
                      name; should read exactly pei_rule)
Data type:           Lookup
Related table:       Rule (pei_rule)        ← select from B01
Column requirement:  Business required
Searchable:          Yes

  ★ Advanced options → Relationship behavior:
     Type:            Configurable cascading
     Delete:          Restrict
                      (★ per spec line 49 — D-07 soft-delete only:
                       a Rule with versions cannot be hard-deleted)
     Reparent / Share / Assign / Merge: leave defaults (Cascade None
       or whatever the portal sets — these are not delete behavior)

Click Save.


─── COLUMN 2: Version Number ────────────────────────────────────

Display name:        Version Number
Schema name:         pei_versionnumber
Data type:           Whole Number
Format:              None (integer)
Minimum value:       1
Maximum value:       (leave default — typically 2,147,483,647)
Column requirement:  Business required
Searchable:          Yes

Click Save.


─── COLUMN 3: Conditions JSON Snapshot ──────────────────────────

Display name:        Conditions JSON Snapshot
Schema name:         pei_conditionsjsonsnapshot
Data type:           Multiple lines of text
Format:              Text (Plain)
Maximum character count:  100000
Column requirement:  Business required
Searchable:          Yes

Click Save.


─── COLUMN 4: Output Snapshot (reuse pei_ruleoutput) ────────────

Display name:        Output Snapshot
Schema name:         pei_outputsnapshot
Data type:           Choice
                     (NOT "Choices" plural — single-select)

  ★ Sync this choice with: (★ THIS IS THE REUSE STEP — DO NOT SKIP)
     → Click the dropdown / "Sync this choice with a new or
       existing choice"
     → Select EXISTING choice: pei_ruleoutput
       (the global Choice you created in B01 with values
        Applicable=890920000, Not Applicable=890920001)
     → Do NOT create a new local Choice.

  If the maker portal blocks reuse (rare — usually it allows
  picking any global Choice), STOP and tell me. We will NOT silently
  create a duplicate local Choice — that breaks single-source-of-
  truth for the binary verdict.

Default value:       (none — leave unset)
Column requirement:  Business required
Searchable:          Yes

Click Save.


═══════════════════════════════════════════════════════════════════
VERIFY BEFORE REPORTING:

  [ ] pei_rule lookup created · target = Rule (pei_rule) · Delete = Restrict
  [ ] pei_versionnumber created · Whole Number · Min 1 · Required
  [ ] pei_conditionsjsonsnapshot created · Multi-line text · 100,000 max · Required
  [ ] pei_outputsnapshot created · Choice · synced to existing
      global pei_ruleoutput (NOT a new local Choice) · Required

═══════════════════════════════════════════════════════════════════
