Update the primary action button and the status pill below it on the Stage 1 (Data Onboarding) page. Do not modify the button color states (blue during work, green on completion), the arrow on the completion state, or the visual positioning of the button and pill.
Button label changes:

Idle state: Change button label from "Data Onboarding" to "Begin Onboarding →"
In-progress states: Keep current labels ("Checking feeds...", "Loading feeds into memory...", etc.)
Completion state: Keep current label "Run Pipeline →" unchanged

Status pill text changes:

Idle state: Change from "Ready · click to begin all 3 sub-stages" to "Ready · runs Check, Load, and Validate in sequence"
Step 1 in progress: Change from "Step 1/3 · Looking for files..." to "Step 1/3 · Check"
Step 2 in progress: Change from "Step 2/3 · Locking files..." to "Step 2/3 · Load"
Step 3 in progress: Change from current text to "Step 3/3 · Validate"
Completion state: Change from "All 3 sub-stages complete — ready to build the risk register" to "All 3 sub-stages complete — ready to run the pipeline"

Keep the existing pill background colors for each state (idle, in-progress, complete). Do not modify the synchronization logic between the button states, sub-stage card states, and pill states.
