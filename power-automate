Update the blue callout box on the Stage 1 (Data Onboarding) page. Do not modify any elements outside this callout box.
Current callout text:
"WHAT HAPPENS WHEN YOU CLICK DATA ONBOARDING — The pipeline runs the three sub-stages below back-to-back — Check → Load → Validate — fully automatically. For each feed it scans the working folder, picks the matching file, opens it into memory, and confirms every required column is present. If a feed is missing, has multiple versions, or fails column validation, the flow pauses on that card so you can drop in the right file or pick a version — your completed steps are preserved and you just press the button again to resume. When all three pillars turn green, the Run Pipeline button unlocks. When you're ready, click the Data Onboarding button below to begin."
Replace with three short paragraphs:
Heading (small caps, bolded, same style as before): "What happens when you click Data Onboarding"
Paragraph 1: "The pipeline runs three sub-stages back-to-back — Check → Load → Validate — fully automatically. See the cards below for what each one does."
Paragraph 2: "If something goes wrong: the flow pauses on the affected card so you can fix the issue (drop in the right file, pick a version, correct columns). Your completed steps are preserved — just press the button again to resume."
Paragraph 3: "When all three sub-stages complete: the next stage, Run Pipeline, becomes available."
Render the three paragraphs with vertical spacing between them so they read as distinct sections rather than one block of text. Bold the lead phrases ("If something goes wrong", "When all three sub-stages complete") and the stage names (Check → Load → Validate, Run Pipeline) consistently. Keep the blue callout box background and border styling unchanged.
