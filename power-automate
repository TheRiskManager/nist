
C7 — CP BU Switch
Inside If no branch of C4, after C6.

Action: Control → Switch
Title: CP BU Switch
On (expression):
outputs('Build_CP_Classifier')
Cases — you need 3 explicit cases + the Default that auto-exists. Add cases by clicking + Add a case twice (Switch starts with one case + default; we need three cases + default).
Case 1:

Case title: Case BU-owned
Equals: bu-owned
Case 2:

Case title: Case Horizontal
Equals: horizontal
Case 3:

Case title: Case Unresolved
Equals: unresolved
Default case (auto-exists):

Rename title: Default Missing
Save. Tell me "done" or paste error.
