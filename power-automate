Update the three sub-stage cards on the Stage 1 (Data Onboarding) page. Do not modify the card layout, the numbered badges, the card titles, the progressive state design (idle → in-progress → complete), or the colors used in any state.
Card 1 — Check
Current text: "Confirm every required Archer .xlsx feed is present in the working folder. If a file is missing or duplicated, the flow pauses here so you can drop the right export in."
Replace with: "Confirms every required source feed is present in the working folder. Pauses here if a file is missing or duplicated."
Card 2 — Load
Current text: "Read each feed into memory and lock the chosen version so nothing changes underneath the pipeline mid-run."
Replace with: "Reads each feed into memory and locks the chosen version so nothing changes underneath the pipeline mid-run."
Card 3 — Validate
Current text: "Verify every feed exposes the columns the downstream stages need. Missing headers stop the flow with a clear, fixable message."
Replace with: "Verifies that every feed has the columns downstream stages need. Stops the flow with a clear, fixable message if headers are missing."
Ensure all three cards use the same typography (sans-serif, regular weight, body text size) consistent with the rest of the page. After applying these changes, the three cards should be visually balanced in height — if they are not, adjust the card container so all three render at the same height regardless of text length.
