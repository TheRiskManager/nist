Next batch — inside Loop Old Cycle IDs, after List Old Staged Records:
Action E: Condition

Left: fx → length(outputs('List_Old_Staged_Records')?['body/value'])
Operator: is greater than
Right: 0
Rename to: Old Cycle Has Records

Inside Old Cycle Has Records → If Yes:
Action F: Apply to each

Select output: fx → outputs('List_Old_Staged_Records')?['body/value']
Rename to: Loop Old Staged Records

Inside Loop Old Staged Records:
Action G: Dataverse → List rows

Table: Staged Records
Filter rows: fx → concat('_pei_importcycle_value eq ', variables('varImportCycleID'), ' and pei_name eq ''', items('Loop_Old_Staged_Records')?['pei_name'], ''' and pei_entitytype eq ', items('Loop_Old_Staged_Records')?['pei_entitytype'])
Rename to: Check Duplicate In New Cycle

This checks if the new cycle already has a Staged Record with the same Name and Entity Type. Tell me when E, F, G are done.
