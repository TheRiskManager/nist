Inside Loop Old Cycles, before Supersede Old Cycle:
Action A:

Variable → Append to array variable
Name: varOldCycleIDs
Value: fx → items('Loop_Old_Cycles')?['pei_importcycleid']
Rename to: Save Old Cycle ID

Then — the Carry Forward block. This goes after ALL 4 entity processing blocks (after App File Exists) but BEFORE Update Import Cycle Counts. Same level as the File Exists conditions.
Action B: Condition

Left: fx → length(variables('varOldCycleIDs'))
Operator: is greater than
Right: 0
Rename to: Old Cycles Had Staged Records

Inside Old Cycles Had Staged Records → If Yes:
Action C: Apply to each

Select output: fx → variables('varOldCycleIDs')
Rename to: Loop Old Cycle IDs

Inside Loop Old Cycle IDs:
Action D: Dataverse → List rows

Table: Staged Records
Filter rows: fx → concat('_pei_importcycle_value eq ', items('Loop_Old_Cycle_IDs'))
Rename to: List Old Staged Records
