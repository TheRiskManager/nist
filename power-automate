Row 1:

Left: fx → length(trim(coalesce(items('Loop_App_Rows')?['Display Name'],'')))
Operator: is greater than
Right: 0


Row 2 (click + Add row, set to AND if not already):

Left: fx → toLower(items('Loop_App_Rows')?['Usage Type'])
Operator: is equal to
Right: fx → 'owner'
