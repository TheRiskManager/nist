================================================================
VISUAL DENSITY CORRECTION — APPLY SOURCE SYSTEMS PATTERN GLOBALLY
================================================================

The Source Systems column on the left has the correct visual treatment for EMS — compact, icon-led, horizontal-row component cards with consistent sizing and tight spacing. This is the template pattern.

APPLY THIS PATTERN TO ALL COMPONENT CARDS ACROSS THE DIAGRAM:

For every component card in every band (User Interfaces, AI Assist Plane, AI Engineering Plane, Automation Plane, Integration Bridge, Data Plane, Content Plane, Reporting), restructure the card to match the Source Systems pattern:

- Small square icon tile on the LEFT of the card, in the product's brand color background
  - Same icon tile size as Source Systems cards (e.g., RSA Archer, GitHub, ServiceNow tiles)
  - Same icon size, same border radius, same internal padding
- Component name as plain text label to the RIGHT of the icon tile
  - Same font size, weight, and color as Source Systems labels
  - Optional secondary text (one-line role) BELOW the name, in muted color, smaller font — only if essential
- Card height matches Source Systems cards exactly
- Card width fills the available column width within its parent Plane container
- Tight vertical spacing between stacked cards within a Plane — match the Source Systems column spacing

REMOVE from the resized cards:
- Floating large product logos centered above text (replace with the icon tile on the left)
- Generic price tags ("$20/user", "$5/user") if any remain
- Excess internal whitespace and padding

RESIZE THE PLANE CONTAINERS to fit the new compact card sizes:
- AI Assist Plane, AI Engineering Plane, Automation Plane, Integration Bridge, Data Plane, Content Plane containers should shrink vertically to fit their content without large empty regions
- Maintain the FOUNDATION / ROADMAP tags on each Plane container exactly as they are now
- Maintain the small CORE / EXTENDED labels or replace with FOUNDATION / ROADMAP if those are already in use
- The Plane title (e.g. "AI Assist Plane", "Automation Plane") stays as the Plane container header

PRESERVE EXACTLY (do not change):
- The overall band structure (USER INTERFACES, AI & INTELLIGENCE, INTEGRATION & AUTOMATION, RECORDS, REPORTING bands)
- The "SECURITY & GOVERNANCE TRUST BOUNDARY" outer dashed boundary
- The Source Systems column on the left (it's already correct — use it as the reference, don't modify it)
- The cross-cutting bottom band (Microsoft Entra ID, Security Roles, Audit Logging, Key Vault)
- All FOUNDATION and ROADMAP tag styling and placement
- Color palette, typography, dashed-vs-solid border conventions, animations, hover states, click-to-focus behavior, side panel structure
- Connection topology (the arrows between components)

The goal is a diagram where every component card has the same visual rhythm as the Source Systems column — compact, scannable, density-honest. Plane containers and band headers preserve their grouping role but shrink to fit the new card sizes.

Verify after restructuring:
- No band has large unused vertical space
- No card has a generic price tag or floating centered logo
- Every component card has the icon-tile-left + name-label-right pattern
- The Source Systems column itself is unchanged (it was already correct)
- The FOUNDATION/ROADMAP visual story remains intact
