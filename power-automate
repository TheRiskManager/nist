Step-by-step
Open Flow 3D → navigate to Loop Orphan Evidence Candidates
You should see inside it:

Applicability Is Orphan (condition) ← this is what we're fixing
Mark Evidence Not Required (inside True)

Add new action BEFORE the condition

Click the + button between the loop boundary and Applicability Is Orphan
Search: Filter array
Add: Data Operation → Filter array
Rename to: Find Matching Applicability

Configure Filter array
From field:
Click fx → paste:
outputs('List_All_Applicability_For_Cycle')?['body/value']
Condition row (one row only):

Left value: click fx → paste: item()?['pei_applicabilityid']
Middle dropdown: is equal to
Right value: click fx → paste: items('Loop_Orphan_Evidence_Candidates')?['_pei_applicability_value']

Save.
Rewrite the 3 rows in Applicability Is Orphan
Delete the existing three expressions and replace with:
Row 1:

Left: fx → first(body('Find_Matching_Applicability'))?['pei_applicabilitystatus']
Middle: is equal to
Right: 890920001

Row 2:

Left: fx → first(body('Find_Matching_Applicability'))?['pei_applicability']
Middle: is equal to
Right: 890920001

Row 3:

Left: fx → first(body('Find_Matching_Applicability'))?['pei_reviewdecision']
Middle: is equal to
Right: 890920000

Top-level operator: Or (unchanged)
Save and verify
