Inside the loop:

Click Add an action inside Build CAP rows
Search for Append to string variable → select it
Configure:

Name: CAPRowsHTML (pick from dropdown)
Value: click into the field, switch to expression (fx if needed) and paste:



<tr><td style="padding: 10px 14px; border-bottom: 1px solid #e1e8f0; font-family: monospace; color: #0A2540; font-size: 14px;">@{items('Build_CAP_rows')?['Title']}</td></tr>
Note: Build_CAP_rows is the loop's internal name (display name with spaces converted to underscores). If hovering the loop header shows a different internal name, use that.
Reply "done" when configured.
