Inside Check RC Removals, add a Condition:

Left box — click fx:

contains(body('Extract_RC_IDs_From_Excel'), items('Check_RC_Removals')?['pei_referencecontrolidentifier'])

Operator: is equal to
Right box — click fx:

false

Rename to: RC Not In File

This checks: "Is this active RC's identifier NOT found in the Excel data?" If true, it was removed.
Say "done".
