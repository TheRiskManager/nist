Update the post-reconciliation elements on the Stage 7 (CAP & BU Reconciliation) page to clean up inconsistencies and align with the page's existing vocabulary and visual style. The reconciliation has run, the 5 sub-stage cards correctly show progression, and the user is on the Review & Edit step. The elements between the sub-stage cards and the review table need refinement.
Do not modify the review table, its filter chips, its columns, or its edit-mode behavior at this time — those will be addressed in a separate prompt. This prompt is scoped to the page-level elements above the table only.

1. Clarify or rename the "Loaded" pill
Between the sub-stage cards and the inputs panel, there is currently a large pill labeled "Loaded". Its purpose is unclear — it looks like a button but the label suggests a state. Determine whether this is:

A button users click to proceed (in which case rename to something like "Continue to Review →")
A status indicator showing the pipeline completed (in which case style it as a status indicator, not a button — smaller, less prominent, with text like "Pipeline run complete")
A leftover element that should be removed

Pick the appropriate interpretation based on the engine logic and remove the ambiguity.

2. Update the small summary pill text
Currently displays: "Reconciled 85 CAP IDs"
Update to reflect the new status vocabulary:
"Reconciled 85 CAPs · [N] Mapped · [N] Needs BU · [N] Archived"
The total stays as 85; the breakdown by status uses the new vocabulary. Pull live counts from the reconciliation output.
If [N] Needs BU > 0, optionally append a second smaller line below:
"[N] new CAPs · [N] retired BUs"
This gives the reviewer an at-a-glance sense of how much pending work they have before they even scroll to the table.

3. Restructure the inputs panel layout
The current inputs panel shows three cards in a 2 + 1 grid (two on top, one below). This is visually asymmetric. Restructure to either:

Option A: Three cards in a horizontal row (preferred if screen width allows)
Option B: Three cards stacked vertically as full-width rows

Either option must present the three inputs in this order, matching the order used in the body intro and callout text:

Fresh CAP list (from Stage 3 output)
Current BU list (from Stage 6)
Previous CAP→BU mapping (uploaded here)


4. Update the labels on each input card
Current labels are inconsistent in tone and length. Update to use a consistent "Source N · what it is · where it comes from" structure:
Card 1:

Filename (existing): [filename].xlsx
Label: "Source 1 · Fresh CAP list · from Stage 3 output"
File path (existing): displayed below the label
Status: ✅

Card 2:

Filename (existing): BU_List.xlsx
Label: "Source 2 · Current BU list · from Stage 6"
File path (existing)
Status: ✅

Card 3:

Filename (existing): [filename].xlsx
Label: "Source 3 · Previous CAP→BU mapping · uploaded here"
File path (existing)
Status: ✅


5. What NOT to change in this prompt

The body intro, the "What happens" callout, and the 5 sub-stage cards
The review table, its filter chips, its columns, the "READ-ONLY" / "Enable editing" toggle, the "Show details" button, or any table-related behavior
The Start button and the progressive disclosure pattern
The engine logic or any backend behavior

These will be addressed in a separate prompt focused specifically on the review table redesign.
