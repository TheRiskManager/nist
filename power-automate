Step C7 — overview
We need to:

Add a new Condition Tier 2 Needed (match count = 0)
Move existing Find BU Mapping for Org Unit action into its If Yes branch
Move existing Mapping Exists Condition (the entire condition with all nested branches) into its If Yes branch

The "move" is done via Power Automate's drag-or-cut-paste-action workflow. Let me handle one piece at a time.

Step C7a: Add Tier 2 Needed Condition
Where: click the + between Tier 1 Ambiguous and Find BU Mapping for Org Unit. Add an action → Condition.
Configure:

Left: fx → paste:

outputs('Compose_Match_Count')

Operator: is equal to
Right: fx → paste:

0
Rename: ⋮ → Rename → Tier 2 Needed
Leave both branches empty for now. We'll fill the If Yes branch by moving existing actions in C7b and C7c.
Confirm done.
(After this, the structure should look like: ...C6 Tier 1 Ambiguous → NEW C7 Tier 2 Needed (empty branches) → existing Find BU Mapping for Org Unit (still where it is) → existing Mapping Exists Condition.)
